<!-- not-user-reviewed -->

# Tasks: List Channel Members

Spec: `context/spec/001-channel-members/` — see `functional-spec.md` and `technical-considerations.md`.

---

- [ ] **Slice 1: Fetch and cache a single channel's member roster (provider tier)**

  > The provider can fetch a channel's members from Slack, persist them per-workspace, serve fast reads within TTL, survive restart, and refresh in the background. No user-facing tool yet; the server still builds and runs.

  - [ ] Add `GetUsersInConversationContext(ctx, *slack.GetUsersInConversationParameters) ([]string, string, error)` to the `SlackAPI` interface and implement it on `MCPSlackClient` in `pkg/provider/api.go`, dispatching edge-first-then-standard like `GetConversationsContext`, and paginating the standard client's cursor to assemble the complete roster. Update any mocks/fakes implementing `SlackAPI` so the package still compiles. **[Agent: go-mcp-backend]**
  - [ ] Add cache types `ChannelMemberRoster` (`MemberIDs []string`, `FetchedAt time.Time`) and `ChannelMembersCache` (`Channels map[string]ChannelMemberRoster`), the `ApiProvider` fields (`membersSnapshot atomic.Pointer`, `membersCachePath`, `refreshingMembers sync.Map`, `lastForcedMembersRefresh map`, `membersMu`, `fetchMembersMu`), the `channelMembersNotReadyMsg` const + `ErrChannelMembersNotReady`, and init in both `newWithXOXP`/`newWithXOXC` (resolve path from `SLACK_MCP_CHANNEL_MEMBERS_CACHE` or `getCachePathWithTeamID(teamID, "channel_members_cache.json")`, seed an empty snapshot). **[Agent: go-mcp-backend]**
  - [ ] Implement provider methods in `pkg/provider/api.go`: `RefreshChannelMembers(ctx)` (boot load from disk, no Slack call), `GetChannelMembers(ctx, channelID)` (snapshot hit within per-entry TTL → return; expired → serve stale + spawn background refresh; absent + in-flight → `ErrChannelMembersNotReady`; absent → synchronous fetch), `fetchAndStoreChannelMembers` (paginate, copy-on-write new map, `atomicWriteFile` under `fetchMembersMu`, empty roster is valid), per-channel background gather guarded by `refreshingMembers.LoadOrStore`, and `ForceRefreshChannelMembers(ctx, channelID)` throttled by `minRefreshInterval` returning `ErrRefreshRateLimited`. Wire `RefreshChannelMembers` into the same boot path that loads users/channels. Route fetch pages through the shared rate limiter + 429 retry. **[Agent: go-mcp-backend]**
  - [ ] Verify: add `pkg/provider` unit tests (testify, mocked `SlackAPI`, `t.TempDir` cache path) covering cold multi-page fetch → complete roster + atomic file write, read hit within TTL with zero Slack calls, per-entry TTL expiry serves stale + refreshes, restart survival via `RefreshChannelMembers`, empty channel → empty valid roster, `ForceRefreshChannelMembers` throttle → `ErrRefreshRateLimited`, concurrent first-time gathers → one fetch + `ErrChannelMembersNotReady`, 429-then-success retry, and two-TeamID workspace isolation. Run `make test` green; then remove any temp/scratch files the check produced. **[Agent: go-mcp-backend]**

- [ ] **Slice 2: Expose the `channels_members` MCP tool (names, filters, refresh)**

  > A user/assistant can call `channels_members` with a channel ID or `#name` and get the complete roster as CSV (UserID, DisplayName, RealName), optionally excluding bots and/or deactivated accounts, with `refresh_cache` support and a clean "not ready" / "not found" message.

  - [ ] Register the tool in `pkg/server/server.go`: add `ToolChannelsMembers = "channels_members"`, append to `ValidToolNames`, and add an unconditional `shouldAddTool(ToolChannelsMembers, enabledTools, "")` registration block with `channel_id` (required), `exclude_bots`, `exclude_deactivated`, `refresh_cache` params and a read-only hint, wired to `channelsHandler.ChannelsMembersHandler`. **[Agent: go-mcp-backend]**
  - [ ] Implement `ChannelsMembersHandler` in `pkg/handler/channels.go` plus the `ChannelMember` CSV struct (`UserID`, `DisplayName`, `RealName`): `IsReady()` gate; resolve `channel_id` (ID or `#`/`@` name) to an ID via a shared resolver (extract/reuse `resolveChannelID` from `conversations.go`) with a clear "channel not found" result on miss; honor `refresh_cache` via `ForceRefreshChannelMembers` (tolerate `ErrRefreshRateLimited`); call `GetChannelMembers`, returning `channelMembersNotReadyMsg` text on `ErrChannelMembersNotReady`; resolve names via `ProvideUsersMap()` (member absent from the users map is still included by ID with blank names); apply `exclude_bots`/`exclude_deactivated` via `IsBot`/`Deleted`; marshal the complete roster to CSV. **[Agent: go-mcp-backend]**
  - [ ] Document the `SLACK_MCP_CHANNEL_MEMBERS_CACHE` env var and the `channels_members` tool in `README.md` (alongside `SLACK_MCP_USERS_CACHE`/`SLACK_MCP_CHANNELS_CACHE` and the tools list) and add it to `.env.dist` if the other cache vars are listed there. **[Agent: go-mcp-backend]**
  - [ ] Verify: add `pkg/handler` unit tests covering ID vs `#name` parity, unknown channel → "not found", default roster includes bot + deactivated + normal + unknown-by-ID (blank names) with correct CSV columns, `exclude_bots`/`exclude_deactivated`/both filters, not-ready path returns the sentinel text, and `refresh_cache=true` triggers a forced refresh (rate-limited → serve cached). Then build the binary (`make build`) and drive it over a stdio MCP handshake to confirm `channels_members` appears in `tools/list` with the four params. Run `make test` green; delete the built binary and any scratch handshake files afterward. **[Agent: go-mcp-backend]**

- [ ] **Slice 3: Feature Testing & Regression**

  > Verifies the whole feature end-to-end against functional-spec.md, run after all implementation slices are complete.
  - [ ] Read functional-spec.md acceptance criteria in full. Generate acceptance-level tests that verify the entire feature as a whole — not individual slices. Cover applicable layers (unit for pure logic, integration for service interactions, e2e for user flows) based on the project's testing stack. Write tests with RED validation (must fail before implementation is confirmed done). Annotate each test with `@spec: 001-channel-members` and `@regression` if suitable for long-term regression. **[Agent: testing-expert]**
  - [ ] Run all generated tests. All must pass. Fix any failures before proceeding. **[Agent: testing-expert]**
