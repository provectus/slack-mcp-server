# Tasks: Agent-Friendly Install & Update (002)

- [x] **Slice 1: Binary reports its version (`--version`)**

  > Prerequisite for every script; smallest user-visible value.
  - [x] Task: Add `String() string` to `pkg/version` returning the 3-line contract (`slack-mcp-server <version>` / `commit: <hash>` / `built: <time>`), with a unit test for the format. **[Agent: go-mcp-backend]**
  - [x] Task: Add `--version` bool flag in `cmd/slack-mcp-server/main.go` (stdlib `flag`, long form only); after `flag.Parse()`, print `version.String()` to stdout and exit 0 before any logger/provider/token validation. **[Agent: go-mcp-backend]**
  - [x] Verify: `make build` then run `build/slack-mcp-server --version` with no Slack env vars set — exit 0, line 1 matches `slack-mcp-server <git-describe-version>`; run `make test`; delete any temp artifacts created during the check. **[Agent: go-mcp-backend]**

- [x] **Slice 2: Fork-native release pipeline (`pv-v*` → GitHub Release with checksums)**

  > After this slice, tagging `pv-vX.Y.Z` produces a correct fork release; upstream tags stay inert.
  - [x] Task: Rework `.github/workflows/release.yaml`: trigger `pv-v*` only, runner `ubuntu-latest`, `make build-all-platforms`, generate `build/checksums.txt` (sha256 of all 6 binaries), upload assets = 6 binaries + `checksums.txt` + `LICENSE`; remove DXT/npm steps and `.env.dist`/`docker-compose.yml` assets. **[Agent: release-infra]**
  - [x] Task: Delete `.github/workflows/release-image.yaml`. Fix Makefile `release` target: validate `pv-v` tag format, push tag to `fork` remote (not `origin`). Delete `npm-publish`, `npm-copy-binaries`, `build-dxt` targets, the `npm/` directory, and `manifest-dxt.json`. **[Agent: release-infra]**
  - [x] Verify: `make build-all-platforms` succeeds locally and produces the 6 expected filenames; the checksum command from the workflow produces a valid `checksums.txt` (`shasum -c` passes); `actionlint` (or careful YAML review if actionlint unavailable) passes on the reworked workflow; `make test` still green; delete generated `build/` artifacts. **[Agent: release-infra]**

- [x] **Slice 3: One-command install script (binary + optional updater)**

  > `curl … | bash` installs a working binary on macOS/Linux without Go.
  - [x] Task: Create `scripts/install.sh`: platform detect (darwin|linux × amd64|arm64), resolve release via hardcoded GitHub API URL (latest or `--version` pin), download binary + `checksums.txt` to temp, sha256 verify, install to `~/.local/bin` (or `--prefix`), `--version`-probe the installed binary, PATH hint (no rc edits), updater prompt via `/dev/tty` with `--with-updater`/`--no-updater` overrides (non-TTY default: install), exit codes 0/2/3/4/5 per tech spec. Installs `scripts/update.sh` as `slack-mcp-update` when accepted (updater may be a stub until Slice 4 — install mechanics only). Testable seams: platform-detect/semver functions sourceable. **[Agent: release-infra]**
  - [x] Task: Create `scripts/test/` plain-sh harness with a curl PATH shim (fixture releases JSON, binary assets, checksums); cover: happy install, `--version` pin, unsupported platform (exit 2), download failure (exit 3), checksum mismatch (exit 4), probe failure (exit 5), updater prompt/flag matrix. **[Agent: release-infra]**
  - [x] Verify: run the harness — all cases pass; `shellcheck scripts/install.sh` clean; run a real install against the shim into a temp `--prefix` using a locally built binary as the fixture asset, confirm `--version` probe output; delete temp dirs and fixtures generated by the run (keep the committed harness). **[Agent: release-infra]**

- [x] **Slice 4: Update script (check, apply, config-change warning, rollback)**

  > `slack-mcp-update` keeps the install current and warns on config-changing releases.
  - [x] Task: Create `scripts/update.sh` (installed as `slack-mcp-update`): binary discovery (sibling / `--bin`), installed version via `--version`, `releases/latest` compare (strip `pv-v`), `--check` mode, apply mode with temp-download → checksum verify → probe → `.bak` swap → rollback on any failure, `CONFIG-CHANGE:` scan over `(installed, latest]` release notes, machine-readable output block (`INSTALLED=`/`LATEST=`/`RESULT=`/`CONFIG_CHANGES=`), exit codes 0/10/1, `$GITHUB_TOKEN` honored. **[Agent: release-infra]**
  - [x] Task: Extend the `scripts/test/` harness: up-to-date, update-available (`--check` exit 10), successful apply (binary really replaced), config-change warning present/absent, and required rollback injections — truncated download, checksum mismatch, failed probe — each asserting old binary restored, visible error, `RESULT=error`, exit 1. Plus the required end-to-end scenario: old installed → newer release with a config-changing release in between → check then apply, all assertions. **[Agent: release-infra]**
  - [x] Verify: run the full harness — all cases green; `shellcheck scripts/update.sh` clean; delete run-generated temp artifacts (keep the committed harness). **[Agent: release-infra]**

- [x] **Slice 5: Service setup from installer (macOS launchd + Linux systemd) and shared run script**

  > `--with-service` gives an always-on server without a git checkout; source flow keeps working.
  - [x] Task: Update `run-with-tokens.sh` binary resolution order: `$SLACK_MCP_BIN` → `~/.local/bin/slack-mcp-server` → script-relative `build/slack-mcp-server`; document precedence caveat in `make reinstall-service` help/README note. **[Agent: release-infra]**
  - [x] Task: Add `--with-service` to `scripts/install.sh`: download `run-with-tokens.sh` to `~/.local/share/slack-mcp-server/`; macOS — render real-path LaunchAgent plist + `launchctl bootstrap`/`kickstart -k`; Linux — render systemd user unit + `daemon-reload` + `enable --now` + linger note; missing token file → install still succeeds with warning pointing to `SLACK_TOKENS_SETUP.md`. Update `scripts/update.sh` to restart whichever service manager is active after a swap. **[Agent: release-infra]**
  - [x] Task: Extend harness: service-mode rendering tests against a temp `$HOME` (assert rendered plist/unit contents and paths; stub `launchctl`/`systemctl` via PATH shims — never touch the real user session), token-file-missing warning path, updater service-restart detection stubs. **[Agent: release-infra]**
  - [x] Verify: run the harness — all green; `shellcheck` clean on changed scripts; confirm rendered plist contains no `/path/to`/`YOUR_USERNAME` placeholders and the systemd unit passes `systemd-analyze verify` when available (skip gracefully on macOS); delete temp `$HOME`s and artifacts. Real launchd/systemd bootstrap stays manual (noted in tech spec). **[Agent: release-infra]**

- [x] **Slice 6: Scripts CI gate and documentation**

  > PRs exercise the script suite; docs make the new path primary.
  - [x] Task: Add a PR workflow (e.g. `.github/workflows/scripts-tests.yaml`): `shellcheck scripts/*.sh run-with-tokens.sh` + run `scripts/test/` harness on `ubuntu-latest` and `macos-latest`. **[Agent: release-infra]**
  - [x] Task: Docs: README new primary "Install" section (curl one-liner, update command, service option, PATH note) and rewritten background-service section covering both flows and both OSes; `docs/02-installation.md` binary-install first; `SLACK_TOKENS_SETUP.md` note re `install.sh --with-service`; `context/product/architecture.md` distribution/CI section updated (fork releases `pv-v*`, no npm/DXT/Docker, scripts CI). **[Agent: release-infra]**
  - [x] Verify: run the harness + shellcheck exactly as the new workflow does (same commands) locally; check README install commands are copy-paste-consistent with actual script flags and URLs (grep script flag names against docs); delete any artifacts. **[Agent: release-infra]**

- [x] **Slice 7: Feature Testing & Regression**

  > Verifies the whole feature end-to-end against functional-spec.md, run after all implementation slices are complete.
  - [x] Read functional-spec.md acceptance criteria in full. Generate acceptance-level tests that verify the entire feature as a whole — not individual slices. Cover applicable layers (unit for pure logic, integration for service interactions, e2e for user flows) based on the project's testing stack. Write tests with RED validation (must fail before implementation is confirmed done). Annotate each test with `@spec: 002-agent-friendly-install` and `@regression` if suitable for long-term regression. **[Agent: testing-expert]**
  - [x] Run all generated tests. All must pass. Fix any failures before proceeding. **[Agent: testing-expert]**
